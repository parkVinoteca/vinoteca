# Vinoteca 제품과 개발 맥락

갱신: 2026-09-18. 최초 점검 기록은 INITIAL_AUDIT.md에 보존하며, 그 문서의 결함 목록은 수정 전 상태다.

### 2026-09-21 현재 작업 우선 기록

운영은 v1.3.2, PR #11의 v1.3.3은 Preview 검증 완료 후 운영 승인 대기다. 아래 날짜별 기록은 과거 구현을 포함한다. 최신 모델·요금·실측은 `AI_MODELS_AND_COSTS.md`를 우선한다.

v1.3.3 라벨 분석은 `wineAnalysis.ts`에서 라벨 원문과 AI 기본 와인 지식을 분리해 읽는다. 검수 상품→완전한 지식 기반 후보→불확실한 상품의 자료 검색 순서다. 후보는 미검증으로 안내하며 공통 검증 캐시에 저장하지 않는다. 와인명·와이너리가 라벨과 연결돼야 하고, 품종 비율을 지식으로 생성하지 않는다. 샤블리 예외 코드는 없다. 실제 전체 사진 두 번 모두 기본6개 필드 일치, Gemini1.642/2.549초, 서버4.1/4.9초, Claude0회. 개인 시음 항목은 자동 입력하지 않았다. AI 소믈리에는 기존 Haiku4.5이며 이번 변경 대상이 아니다.
저장소: https://github.com/parkVinoteca/vinoteca
운영: https://vinoteca-bice.vercel.app

## 제품과 작업 원칙

일본 중심의 와인 애호가용 개인 기록·AI 소믈리에. 한국어/일본어, 모바일 우선, 밝은 아이보리·와인 레드, Cormorant Garamond/Noto Sans JP를 사용한다. 웹/PWA 안정화가 우선이며 결제 연동과 네이티브 앱은 이후 단계다.
사용자는 최초 점검에 따른 필요한 개선 전체의 자율 진행을 승인했다. 로그인과 비밀값 입력은 직접 한다. 브라우저는 Codex 내부 브라우저를 사용하고 불가능할 때만 Chrome을 사용한다. 비밀값은 출력하지 않는다.

## 현재 구현

- Next.js 15.5.24, React 19.3.0, Node 24, TypeScript, Tailwind 3.4.19. 보안 수정 버전으로 갱신했다.
- page.tsx 상태로 홈/테이스팅/블라인드/셀러/소믈리에를 전환한다.
- Supabase 이메일/Google 인증, 비밀번호 재설정, 언어 유지와 친화적 오류 안내.
- 일반 라벨은 /api/label → Gemini gemini-3.5-flash-lite, 소믈리에는 /api/sommelier → Claude claude-sonnet-5 + 웹 검색(최대 3회).
- AI는 사용자 검증, 이미지 크기/형식 검사, DB 사용량 예약, 시간 제한, 응답 검증 후 표시한다. 웹 검색 결과에 없는 가격/블렌딩 출처는 제거한다.
- 취향 점수는 TypeScript로 계산. 일본어/한국어 척도를 같은 값으로 정규화하고 미입력값을 제외한다. 타입별 3개부터 참고값을 표시하고 최근 300개를 30개/100개/이후 구간의 1.0/0.6/0.3 가중치로 집계한다.
- 간단 입력과 전문 입력을 제공한다. 간단 입력은 별점과 평이한 5단계 표현, 전문 입력은 WSET 항목을 유지한다. 스파클링에는 무스, 레드에는 타닌을 표시한다.
- 미각 메모 저장/상세 표시, 전체 기록 기반 통계, 블라인드 저장 후 초기화 및 재개, 중복 저장 방어.
- 사진은 사용자 크롭 후 1024px JPEG로 압축하여 비공개 Storage에 저장. 사용자 소유 경로와 1시간 서명 URL 사용.
- PWA 아이콘·오프라인 안내. API/사진/개인 기록을 서비스워커에 캐시하지 않는다.
- Node 테스트 + PGlite DB 검증, ESLint, typecheck, GitHub Actions 추가.

## 실제 관리 화면에서 확인한 설정

- Vercel Hobby, GitHub main 연동. Supabase Free Tokyo 프로젝트.
- Production/Preview에 Supabase 공개 연결 변수, ANTHROPIC_API_KEY, 사용자가 직접 저장한 GEMINI_API_KEY 존재 확인. 값은 읽지 않았다.
- 기존 NEXT_PUBLIC_GEMINI_API_KEY 변수는 남아 있으나 새 코드에서 사용하지 않는다. 과거 공개된 키의 폐기·재발급 여부는 확인하지 못했다.
- 운영 DB migration `supabase/migrations/20260916_reliability.sql` 적용 완료. 읽기 검증: palate_notes=true, quota 함수=true, anon 함수 실행=false, 클라이언트 사용량 로그 insert=false, 이미지 public=false.
- OAuth 관리 설정 변경 없음. 실제 Google 로그인과 비밀번호 재설정 이메일 전달은 아직 종단 검증하지 않았다.

## 비용 보호와 데이터 보호

운영 DB에 `20260918_product_foundation.sql` 적용 및 읽기 확인 완료. 일본 시간 기준 무료 회원은 테이스팅 월10회·소믈리에 월5회, 유료 회원은 테이스팅 무제한·소믈리에 월50회다. 결제 연동 전에는 모두 무료로 생성한다. 값은 `subscription_limits`에서 조정한다. 전체 프로젝트 월 소믈리에 500·라벨 2000, 동일 기능 호출 간 10초 제한은 유지한다. 실패한 외부 호출도 예약에 포함한다.
RLS 소유자 검사, 블라인드 세션 소유권 검사, 세션/와인 번호 중복 방지, 비공개 이미지. service_role 없이 동작한다.

## 개발과 남은 검증

Node 24 환경에서 npm ci → typecheck/lint/test/build. 키 없이 테스트 및 빌드 가능. 실제 실행에는 개발용 Supabase 연결 설정 필요. README와 .env.example 참고.
이 Mac은 시스템 개발 도구 대신 Codex 번들 Node/Git/Python을 사용했다. 로컬 저장소 `/Users/gaon/Documents/vinoteca`.
로컬 브라우저 검증에는 127.0.0.1:4511의 메모리 DB 모형을 사용했으며 운영 고객 데이터를 변경하지 않았다.
실제 사진으로 두 AI 제공사의 응답 성공, Google OAuth, 복구 메일 전달, iOS/Android 설치 동작은 별도 실제 기기/계정 검증이 남아 있다.
`supabase/migrations/20260918_product_foundation.sql`은 운영 적용 후 PR #7과 함께 배포 완료했다.
사진을 올리고 기록을 취소하면 미사용 파일이 남을 수 있다. 셀러의 대량 사진 서명 요청과 통계 전건 조회는 데이터가 커질 때 페이지별 조회/집계로 개선할 과제다.


## 2026-09-17 분석 실패 및 크롭 후속 개선

실제 운영 재현: Gemini HTTP 429(제공사 사용량 제한), Claude 응답 수신 후 invalid_ai_result. 키/사진/제공사 원문을 노출하지 않는 오류 분류를 먼저 배포했다. 429 오류 메시지에 billing 안내가 포함돼도 결제 오류로 오인하지 않도록 상태 코드 우선 분류한다.
라벨 모델은 단순 문서·이미지 추출용 gemini-3.5-flash-lite로 변경하고 JSON 응답 스키마를 지정한다. 실제 프로젝트 할당량은 별도 검증이 필요하며 모델 변경만으로 한도 복구를 보장하지 않는다.
소믈리에는 검색 인용으로 나뉜 텍스트 조각을 구분자 없이 재조립하고 따옴표/중괄호를 고려해 JSON 하나만 읽는다. 필드별 문자열/숫자 형식을 명시하고 출처 검증은 유지한다.
Gemini는 모델 변경 후에도 실제 HTTP 429가 재현됐다. 명시적인 제공사 할당량/결제 거절일 때만 기존 Claude로 라벨 읽기를 한 번 대체한다. 웹 검색 없이 1024토큰 상한과 JSON 스키마를 사용한다. 사용자 사용량 예약은 한 번이며 인증/시간 초과/이미지 인식 실패는 재시도하지 않는다. 대체 호출은 기존 Anthropic API 요금이 발생하며 Google 할당량 자체를 복구하지는 않는다.
크롭 슬라이더 제거, 44px 모서리 조작점·영역 이동·키보드 지원. 브라우저 내 명도/연결 영역 분석으로 밝은 종이 라벨 후보를 추천한다. OCR이나 실시간 추적은 아니며 확신이 없으면 원본 전체를 제안한다. 외부 호출/비용/의존성 추가 없음. 같은 사진 취소 후 다시 선택할 수 있도록 파일 입력값을 초기화한다.

## 2026-09-17 品種検索・解析進行画面

テイスティングのラベルに品種がなければ、抽出した銘柄・生産者・年をもとに既存Claudeで最大2回のWeb検索を行う。産地の一般的な品種は推測しない。同一銘柄の出典が検索結果にある場合だけ補完し、該当年の確認がなければ比率を表示しない。検索失敗はラベル読取結果を保持し、品種欄の下で資料不足・通信失敗・年の未確認を区別する。出典/比率は今回の入力画面で確認でき、DBには従来通り品種文字列を保存する。追加検索には既存Anthropicの従量費用が発生し、予約はlabel_scanの1回にまとめる。最大3つの外部要求を考慮しlabel関数150秒・ブラウザ145秒に制限する。

両解析画面は写真プレビュー、金色スキャン線、実際のアップロード/解析/結果整理に対応した進行表示を共有する。時間だけで進行度を偽装しない。動きを減らす設定ではスキャンを静止する。キャンセル/画面離脱後の遅い結果を反映しない。開始済みサーバー処理は継続し得る旨を案内する。

2026-09-18 실제 사진 검증에서 검색 URL은 맞지만 품종 하나가 본문과 다른 사례를 발견했다. 품종 보완에 web_fetch(최대 2회, 문서 6000토큰)를 추가하고 실제 가져온 문서의 품종 근거 문구와 이름을 대조한다. 빈티지 근거와 비율도 원문에 있어야 인정한다. 본문을 읽지 못하면 검색 실패를 안내하며 출처 URL만으로 품종을 채우지 않는다. 응답 제한: Gemini 15초, 대체 라벨 Claude 30초, 검색·본문 확인 60초.

## v1.3 후속 변경

평점 통일·모든 평점 기반 취향 분석·유형별 향·5단계 슬라이더·평가 용어·소믈리에 말풍선: `docs/V1_3_TASTING.md` 참고. 사용자 승인으로 `20260919_tasting_v13.sql` 운영 적용 완료(2026-09-19); 컬럼 타입, 평점 범위 제약 존재, RLS 유지 확인. 전문가 점수 자동 검색은 3종 실측 후 사용자 선택으로 OFF. 비용 결과는 `docs/CRITIC_BENCHMARK.md`, 병합/배포 상태는 GitHub PR #8 참조.

## v1.3.1 비용·연속 분석·베타 혜택 (2026-09-20)

후속 v1.3.2 Preview는 PR #10 `codex/verified-wine-identity`에서 진행한다. OCR 원문 전체 읽기 → 검수 상품 연결 또는 상품별 근거에 따른 전체 정보 조사 → 확인 상태/출처 표시를 추가했다. 초기 검수 상품 1종, 검증된 검색 결과만 7일 공통 캐시. 새 사진 분석 시 이전 사진 정보와 출처를 지운다. 테스트 64개 통과. 실제 사진 Preview 검증과 운영 반영은 별도 단계다. 근거와 한계: `docs/WINE_IDENTITY_RELIABILITY.md`.

최신 정책은 `docs/AFFORDABLE_AI.md`, 실측과 단가는 `docs/AI_MODELS_AND_COSTS.md` 참고. 실제 Gemini 키는 결제 관련 거절로 확인했고 Haiku4.5 사진읽기 자동대체를 검증했다. 품종은 Haiku 검색1회와 원문추출, 소믈리에는 Haiku 검색최대2회. Sonnet 자동사용 없음. 10초 강제종료 없음. 베타 free/paid 모두 수동기록 무제한·소믈리에 월20회/일20회 DB 적용 완료. 계정 구분·RLS·프로젝트 안전한도 유지. 사용자 승인에 따른 main/운영 반영은 PR #9. 이전 절의 제한/모델 설명은 당시 기록이다.
