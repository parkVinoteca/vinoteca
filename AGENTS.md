# Vinoteca 작업 지침

- 사용자 질문과 같은 언어로 답한다. 기본 운영 맥락은 일본이며 일본어와 한국어를 지원한다. 기술 용어는 쉬운 말로 설명한다.
- 제품 맥락과 점검 기준은 `docs/VINOTECA_CONTEXT.md`, 최초 발견 사항은 `docs/INITIAL_AUDIT.md`를 먼저 참고한다. 관련 파일부터 읽고 매번 전체 저장소를 재분석하지 않는다.
- 실제 코드와 설정을 우선한다. 설명과 다르면 차이를 보고한다. 확인하지 않은 운영 설정을 추측하지 않는다.
- 목표는 안정적인 웹/PWA, 이후 유료 회원 기능이다. iOS/Android 네이티브 전환은 웹 사용자 검증 후 별도 단계다.
- 합리적인 범위의 수정, 로컬 실행, 테스트, Preview 확인은 자율 진행한다. 브랜치 접두사는 `codex/`다. 계획만 제시하고 멈추지 않는다.
- 다음은 준비와 검증 후 실제 실행 직전에 사용자 명시적 승인을 받는다: Production 배포; main 병합/직접 푸시 또는 저장소 공개 범위 변경; Production DB 데이터 변경/삭제; 스키마 마이그레이션/RLS 변경; OAuth 설정/Redirect URL/권한 변경; 비밀값 입력/교체; 유료 가입/결제/구독/비용 발생; 개인정보 처리방침/이용약관/결제 약관 확정; 앱스토어 등록/제출; 고객 이메일/알림/메시지 발송.
- 비밀값은 채팅/로그에 출력하거나 사용자에게 채팅 입력을 요청하지 않는다. 입력이 필요하면 정확한 화면과 항목을 안내하고 사용자가 직접 입력하도록 멈춘다.
- AI 키를 클라이언트에 노출하지 않는다. 기존 Gemini 클라이언트 직접 호출은 발견된 결함이며 따라야 할 패턴이 아니다. Supabase RLS를 우회하거나 약화하지 않는다.
- 기존 구조와 스타일을 유지하고 신규 문구는 일본어/한국어를 모두 지원한다. 모바일 우선, 키보드 조작, 접근성, 색 대비와 오류 안내를 점검한다.
- 인증/네트워크/AI/업로드 실패를 사용자 친화적으로 처리한다. 성능 개선은 측정 근거로 진행한다.
- 새 의존성은 목적/보안/번들 영향을 검토한다. 외부 유료 SaaS나 추적 도구 도입은 사전 승인을 받는다.
- 구조 설계, 복잡한 버그, 인증/DB/API, 중요 리뷰에 강한 모델을 사용한다. 단순 UI/문구/다국어/반복 작업에는 효율적인 모델과 낮은 추론을 우선한다. Fast Mode는 명시 요청 때만 사용한다. 모델을 실제 변경하지 않았다면 변경했다고 보고하지 않는다.
- 필요한 테스트를 한 번 실행하고 변경/실패 원인이 있을 때만 반복한다. 브라우저 검증은 변경 화면과 핵심 흐름에 집중한다. 대규모 작업은 배포 가능한 단위로 나눈다.
- 매 작업의 완료 보고: 1. 완료한 일 2. 화면에서 확인할 방법 3. 실행한 테스트와 결과 4. 변경 파일 5. 남은 문제/다음 추천 6. 승인 필요 시 이유와 실제 실행 직전 단계. 실패/미검증을 성공으로 보고하지 않는다.
- 최초 점검은 앱 동작을 변경하지 않는다. 회귀 테스트 실패는 기존 결함 재현 결과이며 아직 수정되지 않았다.

## 현재 승인 및 브라우저 선호 (2026-09-16)
- 사용자는 최초 점검에 따른 필요한 개선 작업 전체의 자율 진행을 승인했다. 동일 범위에 대해 반복 승인을 요청하지 않는다. 로그인과 비밀값 입력은 사용자가 직접 한다.
- 브라우저 작업은 항상 Codex/GPT 데스크톱 내부 브라우저를 우선 사용한다. 내부 브라우저에서 처리할 수 없는 특수한 경우에만 Chrome 연결을 사용한다.

## 문제 해결 원칙 (2026-09-21 사용자 요청)
- 모델 상향이나 재시도로 증상을 덮기 전에 프롬프트, 검증 규칙, 호출 순서의 근본 원인을 먼저 조사한다. 수정 전후를 같은 입력으로 비교하고 실제 정확도·소요 시간·API 사용량을 근거로 보고한다.
